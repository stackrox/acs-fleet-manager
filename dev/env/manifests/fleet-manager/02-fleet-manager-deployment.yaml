apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.26.1 (HEAD)
  creationTimestamp: null
  labels:
    io.kompose.service: fleet-manager
  name: fleet-manager
  namespace: "$ACSMS_NAMESPACE"
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: fleet-manager
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert
        kompose.version: 1.26.1 (HEAD)
      creationTimestamp: null
      labels:
        io.kompose.service: fleet-manager
    spec:
      initContainers:
        - name: db-migrate
          image: "$FULL_FLEET_MANAGER_IMAGE"
          imagePullPolicy: IfNotPresent
          command:
            - /usr/local/bin/fleet-manager
            - migrate
          volumeMounts:
            - mountPath: /secrets
              name: secrets
            - mountPath: /config
              name: config
      containers:
        - command:
            - /usr/local/bin/fleet-manager
            - serve
            - --api-server-bindaddress=0.0.0.0:8000
            - --kubeconfig=/secrets/kubeconfig
          image: "$FULL_FLEET_MANAGER_IMAGE"
          imagePullPolicy: IfNotPresent
          name: fleet-manager
          ports:
            - containerPort: 8000
          resources: {}
          volumeMounts:
            - mountPath: /secrets
              name: secrets
            - mountPath: /config
              name: config
      restartPolicy: Always
      volumes:
        - name: secrets
          secret:
            secretName: fleet-manager
            optional: false
        - name: config
          configMap:
            name: config
status: {}
