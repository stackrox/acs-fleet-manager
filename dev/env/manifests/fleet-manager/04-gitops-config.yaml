apiVersion: v1
kind: ConfigMap
metadata:
  name: fleet-manager-gitops-config
  namespace: "$ACSCS_NAMESPACE"
data:
  config.yaml: |
    rhacsOperators:

      crdUrls:
        - https://raw.githubusercontent.com/stackrox/stackrox/4.2.1/operator/bundle/manifests/platform.stackrox.io_securedclusters.yaml
        - https://raw.githubusercontent.com/stackrox/stackrox/4.2.1/operator/bundle/manifests/platform.stackrox.io_centrals.yaml

      operators:

        - deploymentName: "rhacs-operator-4.2.0-395-gf9ab14f1bf"
          image: "quay.io/rhacs-eng/stackrox-operator:4.2.0-395-gf9ab14f1bf"
          centralLabelSelector: "rhacs.redhat.com/version-selector=4.2.0-395-gf9ab14f1bf"
          securedClusterReconcilerEnabled: false
          resources:
            requests:
              cpu: 100m
              memory: 200Mi

    centrals:
      overrides:
        - instanceIds:
            - "*"
          patch: |
            # Set label for all centrals to 4.2.x-395-gf9ab14f1bf
            metadata:
              labels:
                rhacs.redhat.com/version-selector: "4.2.x-395-gf9ab14f1bf"
            # Adjust centrals for development environment
            spec:
              central:
                monitoring:
                  openshift:
                    enabled: false
                resources:
                  limits:
                    cpu: 500m
                    memory: 500Mi
                  requests:
                    cpu: 100m
                    memory: 100Mi
              scanner:
                analyzer:
                  resources:
                    limits:
                      cpu: 500m
                      memory: 500Mi
                    requests:
                      cpu: 100m
                      memory: 100Mi
                  scaling:
                    autoScaling: "Disabled"
                    replicas: 1
                db:
                  resources:
                    limits:
                      cpu: 500m
                      memory: 500Mi
                    requests:
                      cpu: 100m
                      memory: 100Mi
