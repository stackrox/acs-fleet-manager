apiVersion: apps/v1
kind: Deployment
metadata:
  name: dummy
  labels:
    foo: {{ .Values.foo }}
spec:
  replicas: 1
  selector:
    matchLabels:
      foo: {{ .Values.foo }}
  template:
    metadata:
      labels:
        foo: {{ .Values.foo }}
    spec:
      containers:
        - image: quay.io/openshifttest/scratch:latest
          name: test-scratch
